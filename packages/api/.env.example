# Example environment variables for local development
# Replace values as appropriate for your environment.

# Postgres connection (optional — if omitted, app will run in file-backed mode)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/prognos_dev

# MinIO / S3-compatible endpoint for local development
AWS_ENDPOINT=http://localhost:9000
AWS_REGION=us-west-2
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
S3_BUCKET=local-minio-bucket

# Server port
PORT=4000

# Node environment
NODE_ENV=development
